server {
  listen 80 default_server;
  listen [::]:80 default_server;  
  
  index index.html;
  location / {
    proxy_pass  ${API_HOST};  
  }
  location  /admin/ {  
    alias /usr/share/nginx/html/;
    try_files $uri $uri/ /index.html;
  }  
}